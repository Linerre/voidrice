;;; ----------------------------------------------------
;;; ------------------- MISCHELLEOUS -------------------
;;; ----------------------------------------------------
(use-package which-key
  :defer nil
  :config (which-key-mode))

(use-package restart-emacs)

(provide 'init-which-key)
